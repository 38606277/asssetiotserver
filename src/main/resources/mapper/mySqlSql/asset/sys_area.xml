<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sys_area">
	<select id="getTaskIdByParentCode" parameterType="java.lang.String" resultType="Map">
		SELECT  a.code, a.name , a.level_type ,a.merger_name FROM sys_area a  WHERE a.parent_code = #{parentCode}
	</select>
	<select id="getCityByProvince" parameterType="java.lang.String" resultType="Map">
		SELECT  a.code, a.name , a.level_type ,a.merger_name FROM sys_area a  WHERE a.parent_code = #{parentCode}
	</select>

</mapper>
